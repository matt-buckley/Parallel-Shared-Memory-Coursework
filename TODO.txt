CHECK REPORT FIGURE NUMBERS

RUN AGAIN USING A DIFFERENT SEED TO TEST 2300

Test different array contents

Remove global variables

Swap precision and threads to use in command line, as per spec

Implement a thread pool
- https://stackoverflow.com/questions/20167199/reuse-of-threads-pthread
- https://github.com/jonhoo/pthread_pool


Investigate superlinear speedup (e.g. caches)

Comment clock

In report, note:
- That briefly had superlinear speedup due to algorithm differences - max speedup of 19.2, reasonable speedup of 9.7 before using precision flags
- Issues with using only values on the outside, i.e. numbers in the middle don't change even as you scale up
- Neither parallel nor sequential using compile-time optimisations
- Iteration number different than array size